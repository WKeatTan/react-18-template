import blueTheme from 'Container/themes/blueTheme';
import dangerTheme from 'Container/themes/dangerTheme';
import darkTheme from 'Container/themes/darkTheme';
import infoTheme from 'Container/themes/infoTheme';
import lightTheme from 'Container/themes/lightTheme';
import primaryTheme from 'Container/themes/primaryTheme';
import purpleTheme from 'Container/themes/purpleTheme';
import secondaryTheme from 'Container/themes/secondaryTheme';
import successTheme from 'Container/themes/successTheme';
import warningTheme from 'Container/themes/warningTheme';

const AppConfig = {
  locale: 'en',
  defaultTheme: 'blueTheme',
  themes: {
    blueTheme,
    dangerTheme,
    darkTheme,
    infoTheme,
    lightTheme,
    primaryTheme,
    purpleTheme,
    secondaryTheme,
    successTheme,
    warningTheme,
  },
};

export default AppConfig;
